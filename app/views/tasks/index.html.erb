<h1>Task list:</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.name %></li>
  <% end %>
</ul>

<%= link_to "Add a task", new_task_path %>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.name, task_path(task) %></li>
  <% end %>
</ul>
